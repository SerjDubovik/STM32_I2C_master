#include "stm32f10x.h"
#include "GPIO.h"
			

int main(void)
{

	init_GPIO();

    relay_1(off);
    relay_2(off);

    blue_led(off);
    red_led(off);



    // настройка I2C1





	for(;;)
	{
		blue_led(on);

	} // скобка бесконечного цикла

} // скобка мейна
